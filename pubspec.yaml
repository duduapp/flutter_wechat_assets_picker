name: wechat_assets_picker
description: An assets' picker written in pure Dart which looks like the one in WeChat, support multi asset pick from the device.
version: 4.2.1
homepage: https://github.com/fluttercandies/flutter_wechat_assets_picker

environment:
  sdk: ">=2.8.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  extended_image: ^1.1.2
  photo_manager:
    git:
      url: https://github.com/duduapp/flutter_photo_manager.git
      ref: master
  provider: ^4.3.1
  cached_video_player: ^1.0.3
 # video_player: ^0.10.11+2
